# Configuration file for CheckList testing

# Name of test
set(name "CheckList" PARENT_SCOPE)
# Needed files in 'src' directory of the project
set(src "CheckList.cpp" PARENT_SCOPE)
# The same as above, but for 'include' directory
set(include "CheckList.h" PARENT_SCOPE)
# Needed files for test in current directory
set(test "CheckList.cpp" PARENT_SCOPE)
